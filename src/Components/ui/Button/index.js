import React from 'react';
import './style.css'

function Button({title}) {
    return (
        <div>
            <button className="fourth">{title}</button>
        </div>
    )
}

export default Button
